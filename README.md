# WhoWantsToBeAMil
For linux users, run the `make` file.

For windows users:
* Make sure you have : a C/C++ compiler and the C++ GUI framework (https://www.sfml-dev.org/download/)
* Add SFML path to env.
* then run `.exe` file in the .zip.
